# ZeroLauncher
Small python launcher with arguments support for a certain block game

# introduction

This launcher currently only officially supports [MinecraftConsoles](https://github.com/smartcmd/MinecraftConsoles) but it should work with every other client that supports these launch arguments


# USAGE
- install the requirements with `pip install -r requirements.txt`
- run the `launcher.py` file
- enjoy
